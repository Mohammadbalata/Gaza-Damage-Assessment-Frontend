Scan the folder: $ARGUMENTS

Find code duplication (JSX patterns, effect patterns, fetch patterns, form handlers).
For each duplication cluster:
- List files involved
- Show the repeated pattern (abbreviated)
- Propose an extraction target (hook / util / component)
- Estimate lines saved

Output as markdown table. Do not modify files.